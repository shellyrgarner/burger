<h1>Burgers!</h1>

<h2>Burgers Ya Wanna Eat!</h2>

<ul>
    {{#each burgers}}
        {{#unless devoured}}
            {{> devoured=true}}
        {{/unless}}
    {{/each}}
</ul>

<li>
    {{burger_name}}

    <button class="devoured-status" data-id="{{id}}" data-newstatus="{{devoured}}">
        {{#if devoured}}DEVOUR IT!{{/if}}
    </button>
</li>

<ul>
    {{#each burgers}}
        {{#if devoured}}
            {{> devoured=false}}
        {{/if}}
    {{/each}}
</ul>


<h2>Add Ya Burger!</h2>
<form class="create-form">
    <div class="form-group">
        <input type="text" id="addBurger" name="name"> 
    </div>

    <button type="submit">Add Burger</button>
</form>
<h4>Enter a burger you want to eat.  Then click the "Submit" button.</h4>